# Protection CSRF stateful (basée sur la session).
# Choisie plutôt que la variante "stateless" car elle ne dépend pas du
# JavaScript pour fonctionner (formulaires, login) et reste testable côté
# fonctionnel sans navigateur.
framework:
    csrf_protection: true
